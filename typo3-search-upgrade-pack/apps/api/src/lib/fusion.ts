export function rrfFuse(lists:any[][]){const s:any={};const i:any={};const norm=(l:any[])=>{const o:any={};l.forEach((h,idx)=>{const id=String(h.id);o[id]=Math.max(o[id]||0,1/(60+idx+1));});return o;};lists.forEach(l=>{const n=norm(l);l.forEach(h=>{const id=String(h.id);s[id]=(s[id]||0)+(n[id]||0);i[id]=i[id]||h;});});return Object.entries(s).sort((a:any,b:any)=>b[1]-a[1]).map(([id])=>({...i[id],score:s[id]}));}
