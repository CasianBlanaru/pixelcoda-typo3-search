FROM node:20-alpine

WORKDIR /app

COPY index.html simple-api.js ./

ENV NODE_ENV=production \
    PORT=8787 \
    SEARCH_DATA_DIR=/data

RUN mkdir -p /data && chown -R node:node /app /data

USER node

EXPOSE 8787

CMD ["node", "simple-api.js"]
