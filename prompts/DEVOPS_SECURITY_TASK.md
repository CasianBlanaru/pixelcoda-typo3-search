# Cursor Task: DevOps & Security

- Add Dockerfiles for `apps/api` and `apps/worker`, docker-compose for meilisearch + postgres.
- Configure API rate limits, CORS allowlist per project.
- Add HMAC verification middleware for webhooks.
- Provide migrations for `documents`, `chunks (vector)`, `synonyms`, `metrics_*`.
