# Headless integration for GSAP animations
# TODO: Fix TypoScript configuration - currently breaks content rendering
# The DataProcessor needs to be integrated differently with the Headless extension

# Temporarily disabled to prevent breaking content output
# Will be fixed in future update
